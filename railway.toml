[build]
builder = "nixpacks"

[deploy]
startCommand = "python manage.py collectstatic --noinput && python manage.py migrate --noinput && python manage.py seed_large --force && gunicorn core.wsgi:application --bind 0.0.0.0:$PORT --preload --timeout 120 --workers 2"
restartPolicyType = "ON_FAILURE"
